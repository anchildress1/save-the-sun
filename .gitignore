node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

# Vitest browser run artifacts (failure screenshots + attachments)
**/__screenshots__/
.vitest-attachments

# Coverage
coverage

# Lighthouse CI run artifacts
.lighthouseci/

# Ad-hoc screenshots / design refs dropped at the repo root
/*.png
.playwright-mcp/
.shots/

# Sköll voice — QA listening files (the runtime clips are the committed .pcm.b64; the .wav is local-only)
static/audio/skoll/*.wav
